// [DEPRECATED per PRD §4.3] - hidden 2026-05-12
// Teacher's public-storefront editor; storefront model out of scope per
// PRD §1.4. Center settings (PRD §5.2) will replace this.
// Original implementation preserved in git history.

import { notFound } from "next/navigation";

export default function ProfileEditorPage() {
  notFound();
}
