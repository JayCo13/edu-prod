// [DEPRECATED per PRD §4.3] - hidden 2026-05-12
// LMS-coupled live waiting room. The live-session join UI will be rebuilt
// under modules/sessions/ per PRD §5.6 (no course coupling).
// Original implementation preserved in git history.

import { notFound } from "next/navigation";

export default function LiveWaitingRoomPage() {
  notFound();
}
