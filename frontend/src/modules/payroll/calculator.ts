/**
 * Payroll calculator — Phase A stub.
 *
 * Phase B (next cycle) implements the actual math per PRD §5.8 +
 * Appendix A. Until then every call throws so the 15-scenario test
 * suite reports a clean RED.
 */

import type { PayrollInput, PayrollResult } from "./types";

export function calculatePayroll(_input: PayrollInput): PayrollResult {
  throw new Error("calculatePayroll: not implemented (Phase A — TDD red).");
}
